O help de `--runtime` avisa que skill e MCP têm tabelas próprias (ADR 0017): uma chave pode aceitar uma classe e recusar a outra — `cursor` recebe skill mas não MCP, `vscode` o inverso. Antes disso só se descobria com o exit 3.
