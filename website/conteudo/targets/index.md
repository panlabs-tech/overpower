---
title: Targets
description: The 77 runtimes, the two scopes, and where each artifact lands on disk.
---

# Targets

This page covers where overpower writes: the table of runtimes it knows, the two scopes — this repository, or this machine — and what `--global` changes. For each runtime it gives the path an artifact lands on, so you can predict the diff before you accept the plan.
